when:
  - event: ['push', 'manual']
    branch: main

engine: nixery

dependencies:
  nixpkgs/nixos-unstable:
    - nix

environment:
  NIX_CONFIG: |
    experimental-features = nix-command flakes

steps:
  - name: Check
    command: nix flake check
